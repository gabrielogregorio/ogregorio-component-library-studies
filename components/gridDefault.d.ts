import { ReactNode } from 'react';
import React from 'react';
declare type GridDefaultProps = {
    left: ReactNode;
    right: ReactNode;
};
export declare const GridDefault: ({ left, right }: GridDefaultProps) => React.JSX.Element;
export {};

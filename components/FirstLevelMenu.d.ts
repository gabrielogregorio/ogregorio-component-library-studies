import React from 'react';
import { ReactNode } from 'react';
declare type FirstLevelMenuProps = {
    children: ReactNode;
};
export declare const FirstLevelMenu: ({ children }: FirstLevelMenuProps) => React.JSX.Element;
export {};

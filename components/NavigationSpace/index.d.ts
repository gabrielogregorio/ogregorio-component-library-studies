import type { ReactNode } from 'react';
import React from 'react';
interface IProps {
    children?: ReactNode;
}
export declare const NavigationSpace: ({ children }: IProps) => React.JSX.Element;
export {};
